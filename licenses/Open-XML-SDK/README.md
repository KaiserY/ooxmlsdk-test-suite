# Open-XML-SDK Corpus

Source: https://github.com/dotnet/Open-XML-SDK

The corpus files are kept under `corpus/Open-XML-SDK/` with their original
relative paths from the source project.

The corpus was synchronized from Open XML SDK commit
`00967dc871f06776ae969762c6703d062308a6c9`. The checked-in `LICENSE` and
`NOTICE` files match that commit.
